<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sqlMap PUBLIC "-//iBATIS.com//DTD SQL Map 2.0//EN" "http://www.ibatis.com/dtd/sql-map-2.dtd">

<!-- ==========================================================  -->
<!-- Configuration for ibatis sqlmap mapping.                    -->
<!-- ==========================================================  -->

<!-- ======================================================================================================= -->
<!-- This file is generated by bbdalgen (BBD Data Layer Generator), PLEASE DO NOT MODIFY THIS FILE MANUALLY! -->
<!--                                                                                                         -->
<!-- @author Byron Zhang                                                                                     -->
<!-- ======================================================================================================= -->

<sqlMap>
    <!-- ============================================= -->
    <!-- RESULT MAPS                                   -->
    <!-- ============================================= -->
	<resultMap id="EX-RM-SSO-AUTH-RESOURCE" class="com.bbd.bdsso.common.dal.manual.dataobject.ExSsoAuthResourceDO">
        <result property="authId" column="auth_id" javaType="java.lang.Integer" jdbcType="int"/>
        <result property="authName" column="auth_name" javaType="java.lang.String" jdbcType="varchar"/>
    </resultMap>

    <!-- mapped statement for IbatisSsoRoleAuthDAO.fuzzyQueryForPage -->
    <select id="MS-Extra-SSO-AUTH-RESOURCE-FUZZY-QUERY-FOR-PAGE" resultMap="EX-RM-SSO-AUTH-RESOURCE">  
			SELECT DISTINCT
				a.auth_name as auth_name,
				a.id as auth_id
			FROM
				sso_auth a,
				sso_resource b,
				sso_auth_resource c
			WHERE
				a.id = c.auth_id
			AND b.id = c.resource_id
			<dynamic>
				<isNotEmpty prepend="AND" property="key">
					(a.auth_code like concat('%', #key#, '%'))
				</isNotEmpty>
			</dynamic>
			ORDER BY
				c.gmt_modified desc
			LIMIT 
				#offset#, #pageSize#
    </select>

    <!-- mapped statement for IbatisSsoRoleAuthDAO.fuzzyQueryForPage, needed by paging -->
    <select id="MS-BDSSO-Extra-SSO-AUTH-RESOURCE-FUZZY-QUERY-FOR-PAGE-COUNT-FOR-PAGING" resultClass="int" >
			SELECT
				count(distinct(a.auth_code))
			FROM
				sso_auth a,
				sso_resource b,
				sso_auth_resource c
			WHERE
				a.id = c.auth_id
			AND b.id = c.resource_id
			<dynamic>
				<isNotEmpty prepend="AND" property="key">
					(a.auth_code like concat('%', #key#, '%'))
				</isNotEmpty>
			</dynamic>
    </select>

</sqlMap>